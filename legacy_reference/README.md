# Historical exact scripts

These files were extracted from the final implementation records. They preserve the original Google Drive paths, run IDs, runtime restoration steps, source modifications, compatibility repairs, evaluation calls and output checks.

They are intentionally not shortened. Use them to audit the original experiment or recover a matching Colab environment. For a public GitHub interface, prefer the scripts in `../scripts/`.
